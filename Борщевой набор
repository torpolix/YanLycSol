def price(beet, cabbage, potato):
    with open("borsch.txt", "r") as file:
        prices = [float(line.strip()) for line in file]

    total_cost = beet * prices[0] + cabbage * prices[1] + potato * prices[2]
    print(total_cost)
